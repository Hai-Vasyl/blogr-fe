import React from "react"
import { WithStyles } from "../../common/helpers/with-styles"
import styles from "./search.module.scss"

const Search = WithStyles(({ styles }) => {
  return <div>Search</div>
}, styles)

export default Search
