import React from "react"
import { WithStyles } from "../../common/helpers/with-styles"
import styles from "./user-files.module.scss"

const UserFiles = WithStyles(({ styles }) => {
  return <div>UserFiles</div>
}, styles)

export default UserFiles
