import React from "react"
import { WithStyles } from "../../common/helpers/with-styles"
import styles from "./user-stream.module.scss"

const UserStream = WithStyles(({ styles }) => {
  return <div>UserStream</div>
}, styles)

export default UserStream
