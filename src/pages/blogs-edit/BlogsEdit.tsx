import React from "react"
import { WithStyles } from "../../common/helpers/with-styles"
import styles from "./blogs-edit.module.scss"

const BlogsEdit = WithStyles(({ styles }) => {
  return <div>BlogsEdit</div>
}, styles)

export default BlogsEdit
