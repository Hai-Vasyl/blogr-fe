import React from "react"
import { WithStyles } from "../../common/helpers/with-styles"
import styles from "./user-blogs.module.scss"

const UserBlogs = WithStyles(({ styles }) => {
  return <div>UserBlogs</div>
}, styles)

export default UserBlogs
