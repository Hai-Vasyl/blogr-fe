import React from "react"
import { WithStyles } from "../../common/helpers/with-styles"
import styles from "./blogs.module.scss"

const Blogs = WithStyles(({ styles }) => {
  return <div>Blogs</div>
}, styles)

export default Blogs
