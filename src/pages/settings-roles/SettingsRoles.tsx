import React from "react"
import { WithStyles } from "../../common/helpers/with-styles"
import styles from "./settings-roles.module.scss"

const SettingsRoles = WithStyles(({ styles }) => {
  return <div>SettingsRoles</div>
}, styles)

export default SettingsRoles
