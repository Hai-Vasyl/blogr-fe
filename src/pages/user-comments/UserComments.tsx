import React from "react"
import { WithStyles } from "../../common/helpers/with-styles"
import styles from "./user-comments.module.scss"

const UserComments = WithStyles(({ styles }) => {
  return <div>UserComments</div>
}, styles)

export default UserComments
